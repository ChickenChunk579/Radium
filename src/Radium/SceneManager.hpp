#pragma once

#include <Radium/Scene.hpp>
#include <Radium/Application.hpp>

namespace Radium {
    namespace SceneManager {
        inline void LoadScene(const Scene& scene) {
            //currentApplication->registry.clear();
            //scene.AddObjects(currentApplication->registry);
        }


    }
}